#include<stdio.h>
#include<stdlib.h>
#include<string.h>
main()
{
char str[]="Sravya_1255";
char str1[11];
int len=strlen(str);
for(int i=0;i<len;i++)
{
str1[i]=str[i]^0;
printf("%c",str1[i]);
}
printf("\n");
}
